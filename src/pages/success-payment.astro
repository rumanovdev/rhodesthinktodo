---
// Legacy URL from the template era — bookings are requests, not payments.
// Permanently redirect to /booking-received/ keeping the ?booking= reference.
export const prerender = false;
return Astro.redirect(`/booking-received/${Astro.url.search}`, 301);
---
