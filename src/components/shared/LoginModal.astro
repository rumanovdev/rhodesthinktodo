---
// Send the user back to the page they were on after Google sign-in.
const next = Astro.url.pathname + Astro.url.search;
---
<!-- Log In Modal -->
<div class="modal fade" id="login" tabindex="-1" role="dialog" aria-labelledby="loginmodal" aria-hidden="true">
	<div class="modal-dialog" id="loginmodal">
		<div class="modal-content">
			<div class="modal-header justify-content-end border-0 pb-0">
				<a href="#" class="square--30 circle bg-light-danger text-danger" data-bs-dismiss="modal" aria-label="Close"><i class="fa-solid fa-xmark"></i></a>
			</div>

			<div class="modal-body px-4">
				<!-- Heading -->
				<div class="text-center mb-5">
				<h2>Welcome Back</h2>
				<p class="fs-6">Login to manage your account.</p>
				</div>
				<!-- End Heading -->

				<!-- Form -->
				<form class="px-lg-2" action="/api/auth/signin" method="post">

					<!-- Form -->
					<div class="mb-4">
						<label class="form-label" for="email01">Your email</label>
						<input type="email" name="email" class="form-control" id="email01" placeholder="email@site.com" required autocomplete="email">
					</div>
					<!-- End Form -->

					<!-- Form -->
					<div class="mb-4">
						<div class="d-flex justify-content-between align-items-center">
							<label class="form-label" for="pass01">Password</label>
							<a class="link fw-medium text-primary" href="/forgot-password/">Forgot Password?</a>
						</div>

						<div class="input-group-merge">
							<input type="password" name="password" class="form-control" id="pass01" placeholder="8+ characters required" required autocomplete="current-password">
						</div>
					</div>
					<!-- End Form -->

					<div class="d-grid mb-3">
						<button type="submit" class="btn btn-primary fw-medium">Log in</button>
					</div>

					<div class="d-flex align-items-center mb-3"><hr class="flex-grow-1 m-0" /><span class="px-3 text-muted text-md">or</span><hr class="flex-grow-1 m-0" /></div>

					<div class="d-grid mb-3">
						<a href={`/api/auth/google?next=${encodeURIComponent(next)}`} class="btn btn-whites border fw-medium"><i class="fa-brands fa-google me-2"></i>Continue with Google</a>
					</div>

					<div class="text-center">
						<p>Don't have an account yet? <a class="link fw-medium text-primary" href="/register/">Sign up here</a></p>
					</div>
				</form>
				<!-- End Form -->
			</div>

			{/* Brand logos footer strip removed — template filler, not real partners. */}
		</div>
	</div>
</div>
<!-- End Modal -->
